import React from "react";

import { observer } from "mobx-react-lite";

const DeleteAlert = (props) => {


    return (
        <React.Fragment>
         <div>Hacked</div>
        </React.Fragment>
    );
};

export default observer(DeleteAlert);
